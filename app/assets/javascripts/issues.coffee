# TODO: Make more usability
# $(document).on "turbolinks:load", ->
#   auto_position = ->
#     $elem = $('.issues-card.active')
#     top = $elem.first().offset().top - $elem.parent().offset().top
#     $('.issues-card-mono').scrollTop(top)

#   auto_position()
