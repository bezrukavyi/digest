machine:
  ruby:
    version: 2.4.3
database:
  override:
    - bin/rails db:create db:migrate
deployment:
  production:
    branch: production
    commands:
      - bundle exec cap production deploy
